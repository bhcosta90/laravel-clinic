{
    "name": "my-app",
    "private": true,
    "scripts": {
        "dev": "npx concurrently \"cd backend && composer run dev\" \"cd frontend && npm run dev\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}
